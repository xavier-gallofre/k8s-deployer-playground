apiVersion: v1
kind: Service
metadata:
  name: cache
  namespace: demo
  labels:
    app: cache
spec:
  selector:
    app: cache
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
  type: ClusterIP
