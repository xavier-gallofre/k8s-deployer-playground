apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: api-rollout-canary
  namespace: demo
  labels:
    app: api-canary
    strategy: canary
spec:
  replicas: 4
  selector:
    matchLabels:
      app: api-canary
  template:
    metadata:
      labels:
        app: api-canary
    spec:
      containers:
      - name: api
        image: hashicorp/http-echo:0.2.3
        args:
        - "-listen=:8081"
        - "-text=API canary - Hello from microservices demo"
        ports:
        - containerPort: 8081
        env:
        - name: VERSION
          value: "canary"
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
  strategy:
    canary:
      canaryService: api-canary
      stableService: api-stable
      trafficRouting:
        istio:
          virtualServices:
          - name: api-canary-vsvc
            routes:
            - primary
      analysis:
        templates:
        - templateName: success-rate
        startingStep: 2
        args:
        - name: service-name
          value: api-canary.demo.svc.cluster.local
      steps:
      - setWeight: 10
      - pause: {duration: 2m}
      - setWeight: 25
      - pause: {duration: 2m}
      - setWeight: 50
      - pause: {duration: 5m}
      - setWeight: 100
---
apiVersion: v1
kind: Service
metadata:
  name: api-stable
  namespace: demo
spec:
  selector:
    app: api-canary
  ports:
  - port: 8081
    targetPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: api-canary
  namespace: demo
spec:
  selector:
    app: api-canary
  ports:
  - port: 8081
    targetPort: 8081
