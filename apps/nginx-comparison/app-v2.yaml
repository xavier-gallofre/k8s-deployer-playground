apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-comparison-v2
  namespace: demo
  labels:
    app: nginx-comparison
    version: v2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-comparison
      version: v2
  template:
    metadata:
      labels:
        app: nginx-comparison
        version: v2
    spec:
      containers:
      - name: app
        image: hashicorp/http-echo:0.2.3
        args:
        - "-listen=:8080"
        - "-text=App v2 - NGINX vs Traefik comparison"
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: 50m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
